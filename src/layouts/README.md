# Layouts

This directory contains master layout files (e.g., `main.html` or `base.html`) that wrap around the page contents.
They typically include the `<head>` tag, the main structure, and global scripts/stylesheets.
